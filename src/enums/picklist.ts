export enum EPickListStatus {
  Draft = 1,
  InProgress = 2,
  Picked = 3,
  Validated = 4,
}

/** Line-level status (picklist detail lines) */
export enum EPickListLineStatus {
  Waiting = 1,
  Picked = 2,
  Validated = 3,
}
